# Wave 1 canaries

This folder contains the two stateless HTTP canaries used to validate routing,
resource constraints and service-scoped rollback on the VM. They require no
runtime secret or persistent state.
