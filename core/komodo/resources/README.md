# Komodo resources

`stacks.toml` is the Git source for the self-managed `polinetwork-vm` Resource
Sync and the `core` and `applications` Stacks. Seed that Resource Sync once in a
clean Komodo database; subsequent changes are reviewed and applied from Git.

Stack deployment is intentionally not automatic while the legacy Compose
projects remain on `vm01`.
