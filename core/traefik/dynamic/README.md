# Traefik dynamic configuration

This directory contains Git-backed file-provider configuration loaded by the
Traefik service. `openbao-tls.yaml` defines certificate verification for the
internal HTTPS connection to OpenBao.
