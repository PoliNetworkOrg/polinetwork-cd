apiVersion: apps/v1
kind: Deployment
metadata:
  name: bot-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bot-dev
  template:
    metadata:
      labels:
        app: bot-dev
    spec:
      containers:
        - name: bot-dev
          image: polinetwork/botcsharp_dev:2f438fae9e8189afccd7874ca04420155009f690
          # command: [ "sleep", "10000" ]
          imagePullPolicy: IfNotPresent
          volumeMounts:
          - name: config
            mountPath: /config
          - name: tz-config
            mountPath: /etc/localtime
      volumes:
        - name: config
          secret:
            secretName: bot-config-secret
        - name: tz-config
          hostPath:
            path: /usr/share/zoneinfo/Europe/Rome
            type: File
      

